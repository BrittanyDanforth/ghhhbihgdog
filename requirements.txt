# GhostSpiral v10.7 - Python Dependencies
# Install: pip install -r requirements.txt

# === CORE (required for main pipeline) ===
requests[socks]>=2.28    # HTTP library + SOCKS proxy support (via PySocks)
PySocks>=1.7             # SOCKS5 proxy support for requests library
tenacity>=8.0            # Retry logic with exponential backoff
stem>=1.8                # Tor control protocol (NEWNYM circuit rotation)
monero>=1.1              # Monero wallet RPC client (monero-python)
psutil>=5.9              # System resource monitoring

# === OPTIONAL (for specific modules) ===
python-gnupg>=0.5        # GPG encryption (thor_swap_preparer --gpg-recipient)
pycryptodomex>=3.19      # AES-GCM encryption (SML, dmswitch)
qrcode>=7.0              # QR code display (dmswitch)
aiohttp>=3.9             # Async HTTP (testergatherSystem)
aiohttp-socks>=0.8       # SOCKS proxy for aiohttp (testergatherSystem decoy traffic)
beautifulsoup4>=4.12     # HTML parsing (collectgrab)
numpy>=1.24              # Numerical ops (testergatherSystem AI detection)
cryptography>=41.0       # Fernet encryption (testergatherSystem)
pyyaml>=6.0              # YAML config (tor_endpoint_juggler, swap_retry_guard)
