requests>=2.28
# REQUIRED for socks5h:// proxying. Without it every Tor-routed request raises
# InvalidSchema("Missing dependencies for SOCKS support") -- requests cannot
# speak SOCKS on its own, so the whole toolchain refuses to run. It was missing
# from this list, which meant a clean install could not use Tor at all.
PySocks>=1.7.1
# REQUIRED for the Pi doorbell and the wake agent. gs_wake_proto boxes every
# wake message with libsodium crypto_box (X25519 + XSalsa20-Poly1305). Measured
# against PyNaCl 1.6.2: Box.encrypt() draws its own 24-byte nonce and returns
# nonce+ciphertext+MAC, 40 bytes of overhead, and sodium_pad puts every record
# on exactly one 256-byte block so the job cannot be read off the wire by
# length. There is NO unauthenticated wake mode and no degraded mode -- unlike
# --gpg-recipient, where a meaningful unencrypted state exists, "an
# unauthenticated wake note" is a magic packet with extra steps. Without this
# the doorbell refuses to start.
PyNaCl>=1.5
tenacity>=8.0
stem>=1.8
monero>=0.8
psutil>=5.9
python-gnupg>=0.5
# PyYAML WAS HERE AND NOTHING IMPORTED IT. Not one `import yaml` anywhere in
# the repository -- the shipped tools, the suites, or the testnet drivers.
# systemd/gs-wake-agent.service already said so in passing ("stem, gnupg and
# yaml are deliberately NOT here: they are guarded or unused on this path"),
# and it was still an install requirement.
#
# On a vault that holds a spend key, a package installed for nothing is
# attack surface bought for nothing. Removed, and tests/test_ipleak.py now
# checks the direction that let it sit here: every line in this file must be
# a module something actually imports, or be listed as a justified exception.
# Dev/test only: the suite runs pyflakes over every shipped tool to prove no
# undefined names. Coverage measured 214 warning/abort lines that no test ever
# executes, so a NameError in one of them is invisible to the suite -- a static
# check over all of them is the only realistic guarantee. The check SKIPS if
# this is absent rather than failing.
pyflakes>=3.0
