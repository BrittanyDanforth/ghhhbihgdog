# GhostSpiral — Python Dependencies
# Install everything: pip install -r requirements.txt
# Or use the installer: bash install.sh

# --- CORE (required for main pipeline) ---
requests[socks]
PySocks
tenacity
stem
monero
psutil

# --- OPSEC / MODULES ---
python-gnupg
pycryptodomex
qrcode
pyyaml

# --- INTEL / OPTIONAL ---
beautifulsoup4
aiohttp
aiohttp-socks
