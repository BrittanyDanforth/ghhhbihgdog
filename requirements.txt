# GhostSpiral v10.7 — Python Dependencies
# Install: pip install -r requirements.txt
# Python 3.10+ required

# ═══════════════════════════════════════════════════════
# CORE — Required for the main pipeline
# ═══════════════════════════════════════════════════════
requests[socks]>=2.28    # HTTP + SOCKS5h proxy support
PySocks>=1.7             # SOCKS5 proxy backend for requests
tenacity>=8.0            # Retry with exponential backoff
stem>=1.8                # Tor control protocol (NEWNYM)
monero>=1.1              # Monero wallet RPC (monero-python)
psutil>=5.9              # System resource monitoring

# ═══════════════════════════════════════════════════════
# OPSEC TOOLS — Used by specific modules
# ═══════════════════════════════════════════════════════
python-gnupg>=0.5        # GPG encryption (thor_swap_preparer)
pycryptodomex>=3.19      # AES-GCM (SML, dmswitch, integrity_faker)
qrcode>=7.0              # QR code display (dmswitch)

# ═══════════════════════════════════════════════════════
# CHAOS MODULES — Optional, for extended toolkit
# ═══════════════════════════════════════════════════════
pyyaml>=6.0              # YAML configs (tor_endpoint_juggler, swap_retry_guard)

# ═══════════════════════════════════════════════════════
# INTELLIGENCE — For collectgrab / testergatherSystem
# ═══════════════════════════════════════════════════════
beautifulsoup4>=4.12     # HTML parsing (collectgrab)
aiohttp>=3.9             # Async HTTP (testergatherSystem)
aiohttp-socks>=0.8       # SOCKS proxy for aiohttp
numpy>=1.24              # Numerical ops (testergatherSystem)
cryptography>=41.0       # Fernet encryption (testergatherSystem)

# ═══════════════════════════════════════════════════════
# OPTIONAL — Uncomment if needed
# ═══════════════════════════════════════════════════════
# telethon>=1.34         # Telegram client (testergatherSystem)
# selenium>=4.15         # Browser automation (collectgrab, unused)
# yubikey-manager>=5.0   # YubiKey support (idk)
